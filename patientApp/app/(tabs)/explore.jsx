import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function TabTwoScreen() {
  return (
    <SafeAreaView>
      <Text className="bg-cyan-300 font-bold ">This is explore page</Text>
    </SafeAreaView>
  );
}
